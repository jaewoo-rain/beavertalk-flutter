/// 홈의 두 모드 — Figma `Home/Header-Mode` 의 `Mode=학습|대화`.
///
/// ⛔ 서버의 코스(표현학습·프리토킹)와 **다른 축이다.** 사장님 정의(2026-09-22):
/// - 학습 = 정해진 커리큘럼(표현학습 → 실전대화). 서버가 진도로 정한다(`auto`).
/// - 대화 = 제한 없는 자유 대화. 진도 게이트가 없다(`normal`).
enum HomeMode { learn, talk }
