# .herdr-checkpoint — bt-front

작성: 2026-08-05 / herdr 서버 재시작 대비

---

## [정체성 / 역할]

- **이름**: `bt-front` — 판넬 `w5:p1`, 탭 `w5:t1`
- **역할**: BeaverTalk **프론트엔드(Flutter) 오케스트레이터**. 경로 `C:\Users\jaewoo\Desktop\beavertalkweb\beavertalk-flutter`
- **담당 계층**: 네이티브(`packages/**`, `android/**`, `ios/**`) + 아바타 UI + 실기기 측정. Dart 계층은 하위 에이전트 담당
- **운영 매뉴얼**: 착수 전 grep 확인 → 계획 → 실행 → anti-ai-slop/반응형 준수 → analyze·test 2회 연속 그린 → [핵심결과/변경파일/검증결과] 3줄 보고. 되돌리기 어려운 건 먼저 확인

### 주변 에이전트 (재시작 후 이름 재등록 필요할 수 있음)
| 이름 | 판넬 | cwd | 관계 |
|---|---|---|---|
| `bt-back` | `w5:p2` | `beavertalk-server` | **상위 오케스트레이터.** 내 보고 대상 |
| `bt-cascade` | `w5:p5` (탭 `w5:t5`) | `beavertalk-flutter-cascade` | **내 하위.** Dart 계층 담당 |

- 보고 명령: `herdr agent prompt <이름> "<내용>"`
- ⚠ **백틱을 bash 이중따옴표 안에 쓰지 마라** — 명령치환돼 식별자가 지워진다. 이 세션에서 두 번 당했다. **파일에 쓰고 `"$(cat 파일)"` 로 넘겨라**
- ⚠ 상대가 `blocked`(선택 UI 표시 중)이면 **보내지 마라** — Enter 가 선택을 건드린다. `herdr agent get <이름>` 으로 먼저 확인

---

## [지금 뭐 하고 있었나]

bt-cascade 의 **Dart 계층 1차 덩어리 커밋 4건을 R6 규칙대로 직접 검증**하는 중이었다.

---

## [어디까지 완료]

### 조사 (완료)
- `docs/2026-08-05_1720_barge-in-aec-feasibility.md` — barge-in 3종 타당성. **AEC 결론 = 부분적**
- `docs/2026-08-05_1748_cascade-contract-client-impact.md` — 계약 확정본 클라 영향 (bt-cascade 작성, 메인에 동기화됨)
- `docs/2026-08-05_1758_cascade-dart-layer-plan.md` — Dart 계층 설계 (cascade 워크트리에만 있음, **미동기화**)

### 커밋 (cascade 워크트리 `feat/cascade-barge-in`, **푸시 안 함**)
```
c612c8b feat(통화): audio_cancel 로 비버를 끊는 경로 — 취소 잔여가 턴을 되살리고 쿠션을 부풀리던 문제까지
85a55c5 refactor(통화): _beaverSpeaking 이 마이크·재생·아바타를 겸직하던 것 — 저장은 하나, 해석만 분리
1994206 feat(통화): 재생 진행도에서 무음 필러를 빼는 원장 — 헤드 위치만 세면 안 들은 침묵까지 센다
921dcd0 feat(통화): 재생 버퍼를 죽이지 않고 비우는 경로 — barge-in 은 release() 로는 못 끊는다
```
(분기점 = `0e95f3b`, feat/multilang)

### 내가 직접 검증한 것 ✅
- `flutter analyze lib/ test/` → **No issues found**
- 커밋 구조: 4건 전부 `lib/**`·`test/**` 만. **파일 소유권 지켜짐**
- **비동기 경합 수정 실물 확인** — `_clearGen` 세대 카운터를 `await` 직전 캡처 → 반환 후 비교 → clear 가 끼어들었으면 앵커/꼬리 갱신 건너뜀. 오디오·무음 **양쪽 분기 모두** 적용. `_clearPlayback()` 의 Dart 리셋도 전부 `await` 이전. 진짜다

### 두 모드 테스트 — **완료됨 ✅** (종료 직전에 끝났다)
커밋된 HEAD(`c612c8b`) 에서 내가 직접 재실행:
```
flutter test                                     → 232건 통과 (exit 0)
flutter test --dart-define=CASCADE_BARGE_IN=true → 232건 통과 (exit 0)
```
**1차 덩어리 검증은 전부 닫혔다.** bt-cascade 보고값(232/232)과 일치.

⚠ 한계 명시: 이건 **위젯/단위 테스트가 그린이라는 뜻이지 실기기에서 barge-in 이 동작한다는 뜻이 아니다.**
상시개방 경로는 네이티브 `clear()` 가 아직 없어 폐기가 폴백으로 떨어지고, 에코는 실기기 측정 전이다.
실동작 검증은 **에코 측정 리그 + 네이티브 작업 이후**다.

---

## [다음 이어서 할 일]

1. ~~두 모드 테스트 재실행~~ — **완료. 재시작 후 다시 돌릴 필요 없다.** bt-back 에도 보고 완료
2. **A/B/C 범위 결정 대기 중** — 결정 나면 내 네이티브 트랙 착수:
   - 에코 측정 리그 (트랙 맨 앞, 2~3일)
   - Android AEC 정비 — AudioTrack 속성 `USAGE_MEDIA`→`USAGE_VOICE_COMMUNICATION`, `MODE_IN_COMMUNICATION`, `AcousticEchoCanceler` 명시 부착
   - 플러그인 `clear()` 네이티브 구현 (계약은 이미 확정, 아래 참조)
   - 아바타 UI 재배선 확인 (bt-cascade 가 재생상태로 옮겨놨으니 실동작만 확인)
3. bt-cascade 는 **"새로 벌리지 말고 대기"** 상태. 범위 결정 나면 지시 필요

### 확정된 네이티브 clear() 계약 (내가 구현할 것)
```
채널: flutter_pcm_sound/methods   메서드: "clear"   인자: 없음
반환: Map { "frames_discarded": int }
```
- 단위 = **입력 프레임(PCM24k)**. 출력 프레임 아님
- **대기 큐 + 트랙 미방출분 모두 포함** (큐만 세면 call_id=855 함정 재발)
- flush **직전** 값. 서버발/필러 구분 안 함(그건 Dart `PlaybackLedger` 몫)
- 트랙은 살려 둠(release 하지 않음), 무음 keep-alive 유지
- 소비측 어댑터는 이미 존재: `lib/features/normalcall/data/datasources/pcm_playback_control.dart` (int·Map 둘 다 수용, `MissingPluginException` 흡수)

---

## [열린 결정]

| # | 내용 | 상태 |
|---|---|---|
| **1** | **A/B/C 범위** — A: 음성 barge-in 풀세트(16~22일, 현재 진행) / **B: 탭해서 끊기(4~5일, AEC 불필요)** / C: 진행도 생략(12~17일) | **사장님 답 대기.** 내가 B 권했음 |
| **2** | `CLAUDE.md` 를 커밋할까 | **사장님 답 대기.** 양쪽 워크트리에서 `M` 상태. 승인 범위가 "캐스케이드 작업물"이라 임의로 안 함 |
| 3 | `docs/2026-08-05_1758_...` 를 메인 워크트리로 동기화 | 미실행. gitignore 라 수동 복사 필요 |

### 이미 닫힌 결정 (재논의 금지)
- `turn_end` = **TTS 오디오 마지막 바이트 송신 후**. 계약 불변
- `audio_cancel` — epoch 헤더 없음, **턴 종결 겸함**, turn_id 필수
- `user_turn_start/end` — **서버→클라 통지**, UI 표시용. 클라 VAD 없음
- `played_server_bytes` — 서버발 바이트 중 실제 방출분. 클라 자체 필러 제외. **Dart 외삽값 송신 금지**
- 마이크 상시개방 = **하드 게이트**. (a)AEC 정비 머지 (b)에코 측정 파라미터 확정 이후에만 ON. 현재 `CASCADE_BARGE_IN` 플래그 뒤, **기본 false**
- 쿠션 인플레이션 — 서버가 100ms 고정간격+무음패딩으로 흡수. **누적송출 ≤ 실시간 레이트 보장은 서버 책임**
- `origin/dev` = **죽은 라인** (main 대비 118/0, 완전 흡수). 에코 백포트 불필요
- 에코 수정 `f641536` 은 `origin/main`(PR #8)·`feat/multilang`·`feat/cascade-barge-in` 에 **있음**

---

## [건드린 파일]

### 메인 워크트리 `beavertalk-flutter` (브랜치 `feat/multilang`, HEAD `0e95f3b`)
- `CLAUDE.md` — **M (미커밋)**. R5~R8 추가:
  - R5 보고 규약(결론은 push, 형식·트리거)
  - R6 하위 보고 검증 없이 올리지 마라 + 숫자도 1차 자료 확인 + 확인 못 한 건 명시
  - R7 Claude 는 항상 `--dangerously-skip-permissions`
  - R8 gitignore 파일(`docs/`, `.env`)은 워크트리에 안 따라옴
- `docs/2026-08-05_1720_barge-in-aec-feasibility.md` — 신규(gitignore, 미추적)
- `docs/2026-08-05_1748_cascade-contract-client-impact.md` — cascade 에서 복사(gitignore)
- `.herdr-checkpoint.md` — 이 파일
- ⚠ **세션 초반에 `feat/multilang` 을 origin 에 푸시함**(`8e227ad..0e95f3b`, 커밋 8건). 그 뒤 PR #8 로 main 에 머지됨

### cascade 워크트리 `beavertalk-flutter-cascade` (브랜치 `feat/cascade-barge-in`)
- **커밋됨**: `normalcall_controller.dart`, `playback_ledger.dart`, `pcm_playback_control.dart`, `test/playback_ledger_test.dart`
- **미커밋 M**: `CLAUDE.md`(내가 복사), `linux/`·`macos/`·`windows/` 의 `generated_plugin_registrant*` (flutter test 실행 부산물, 무시해도 됨)
- `docs/` 15개 + `.env` — 내가 수동 복사(gitignore 라 워크트리에 안 따라옴)

### 손대지 않음
`packages/**`, `android/**`, `ios/**` — 내 트랙이지만 **아직 한 줄도 안 건드렸다**

---

## [재시작 후 첫 3분에 할 일]

```bash
# 1. 에이전트 이름 재등록 확인 (재시작으로 풀렸을 수 있음)
herdr agent list
# 풀렸으면: herdr agent rename <pane_id> bt-front  (bt-back, bt-cascade 도 동일)

# 2. 미완 검증 재실행
cd C:/Users/jaewoo/Desktop/beavertalkweb/beavertalk-flutter-cascade
flutter test && flutter test --dart-define=CASCADE_BARGE_IN=true

# 3. 결과를 bt-back 에 보고 (파일 경유, 백틱 금지)
```
그리고 **사장님께 열린 결정 1·2 를 다시 물어라.** 답 없이 진행하지 마라.
